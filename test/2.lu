float glob = 12.0

lambda float get_glob() -> glob

lambda float mutate_a(float b) -> {
        int a = 12
        return b+a
}

lambda float mutate_glob(float a) -> {
    mutate_a(a+b)
    return a
}


lambda string stest(string a) -> {
    return a
}